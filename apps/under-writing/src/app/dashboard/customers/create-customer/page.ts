import type { Metadata } from 'next';

import CreateCustomer from '~/lib/pages/dashboard/customers/CreateCustomer';

export const metadata: Metadata = {
  title: 'Create Customer',
};

export default CreateCustomer;
