import type { Metadata } from 'next';

import CreateDefaultPenalty from '~/lib/pages/dashboard/products/CreateDefault';

export const metadata: Metadata = {
  title: 'Create Default Penalty',
};

export default CreateDefaultPenalty;
