import type { Metadata } from 'next';

import DefaultPenalty from '~/lib/pages/dashboard/products/DefaultPenalty';

export const metadata: Metadata = {
  title: 'Default Penalty',
};

export default DefaultPenalty;
