{
  "buildCommand": "pwd && ls -la && ls -la apps/ && cd apps/under-writing && pwd && NODE_PATH=../../node_modules npx next build",
  "outputDirectory": "apps/under-writing/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
